# makeloop: generated loop artifacts + local-only files
.loop/
.local/
.DS_Store
docs/

# Local agent / persona files — never publish (self-contained; do not rely on global gitignore)
.persona.md
.codex/
.claude/settings.local.json

# >>> project-bootstrap local-only >>>
CLAUDE.local.md
.codex/config.toml
.local/
.cc-status-bar.voice.json
# <<< project-bootstrap local-only <<<
